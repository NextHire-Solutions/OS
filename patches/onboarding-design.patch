diff --git a/orchestrator/app/globals.css b/orchestrator/app/globals.css
index d247498..a222f37 100644
--- a/orchestrator/app/globals.css
+++ b/orchestrator/app/globals.css
@@ -1,43 +1,55 @@
 /* ============================================================
-   Corofy Orchestrator — design system (BrokerStaffer light)
-   Clean white ops-console on brokerstaffer.com branding:
-   brand blue #2568EF, ink #232323, DM Sans + Oswald display.
+   Corofy Orchestrator — design system
+
+   Aligned to the BrokerStaffer workspace so this tool sits beside
+   the other four without looking like a different product.
+
+   ONLY TOKEN VALUES CHANGED. Every token name, class name and rule
+   is the one that was here before, so all 21 components and 8 pages
+   render exactly as they did — same layout, same spacing, same
+   structure, in the workspace's palette and type.
+
+     blue    #2568EF -> #0165FE   (the logo blue)
+     ink     #20242E -> #0D1526
+     ground  #F6F8FB -> #EAECEF
+     radius  12/8px  -> 14/10px
+     type    DM Sans + Oswald -> Inter
    ============================================================ */
 
 :root {
   /* surfaces */
-  --bg: #f6f8fb;
-  --surface: #ffffff;
-  --surface-2: #f4f6fa;
-  --surface-3: #eaeef5;
-  --border: #e6e9f0;
-  --border-strong: #d3d9e4;
+  --bg: #EAECEF;
+  --surface: #FFFFFF;
+  --surface-2: #F4F5F7;
+  --surface-3: #EFF1F4;
+  --border: #E7E9ED;
+  --border-strong: #DDE1E7;
 
   /* text */
-  --text: #20242e;
-  --text-2: #3f4654;
-  --muted: #69727d;
+  --text: #0D1526;
+  --text-2: #3D4657;
+  --muted: #7A8394;
 
   /* brand + semantics */
-  --accent: #2568ef;
-  --accent-deep: #1e56c8;
-  --accent-soft: rgba(37, 104, 239, 0.09);
-  --green: #178a50;
-  --green-soft: rgba(23, 165, 90, 0.11);
-  --amber: #a16207;
-  --amber-soft: rgba(217, 155, 30, 0.14);
-  --red: #d33131;
-  --red-soft: rgba(220, 60, 60, 0.10);
+  --accent: #0165FE;
+  --accent-deep: #0159E0;
+  --accent-soft: #E8F0FF;
+  --green: #0E8A5F;
+  --green-soft: #DFF6EA;
+  --amber: #B45309;
+  --amber-soft: #FFF2E2;
+  --red: #C93A31;
+  --red-soft: #FDE7E5;
   --cyan: #0b7a95;
   --cyan-soft: rgba(14, 145, 178, 0.11);
-  --violet: #6d4fd8;
-  --violet-soft: rgba(122, 92, 230, 0.11);
+  --violet: #5B3FD4;
+  --violet-soft: #EFEBFF;
 
-  --radius: 12px;
-  --radius-sm: 8px;
-  --shadow: 0 1px 2px rgba(16, 24, 40, 0.05), 0 6px 20px -8px rgba(16, 24, 40, 0.08);
-  --shadow-lift: 0 2px 4px rgba(16, 24, 40, 0.06), 0 12px 32px -10px rgba(16, 24, 40, 0.14);
-  --ring: 0 0 0 3px rgba(37, 104, 239, 0.18);
+  --radius: 14px;
+  --radius-sm: 10px;
+  --shadow: 0 1px 2px rgba(13,21,38,.04), 0 8px 24px -12px rgba(13,21,38,.10);
+  --shadow-lift: 0 1px 3px rgba(13,21,38,.06), 0 12px 28px -10px rgba(13,21,38,.16);
+  --ring: 0 0 0 3px rgba(1,101,254,.12);
 }
 
 * { box-sizing: border-box; }
@@ -47,7 +59,7 @@ html, body {
   padding: 0;
   background: var(--bg);
   color: var(--text);
-  font: 14px/1.55 var(--font-sans, "DM Sans"), -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
+  font: 14px/1.5 var(--font-sans, "Inter"), -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
   -webkit-font-smoothing: antialiased;
   text-rendering: optimizeLegibility;
 }
@@ -116,7 +128,7 @@ code {
 /* ---------- typography ---------- */
 
 h1 {
-  font-family: var(--font-display, "Oswald"), var(--font-sans, "DM Sans"), sans-serif;
+  font-family: var(--font-sans, "Inter"), -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
   font-size: 24px;
   font-weight: 600;
   letter-spacing: 0.01em;
diff --git a/orchestrator/app/layout.tsx b/orchestrator/app/layout.tsx
index c95202f..988c8f4 100644
--- a/orchestrator/app/layout.tsx
+++ b/orchestrator/app/layout.tsx
@@ -1,10 +1,15 @@
 import "./globals.css";
 import type { ReactNode } from "react";
-import { DM_Sans, Oswald } from "next/font/google";
+import { Inter } from "next/font/google";
 import NavLinks from "@/components/NavLinks";
 
-const dmSans = DM_Sans({ subsets: ["latin"], weight: ["400", "500", "600", "700"], variable: "--font-sans" });
-const oswald = Oswald({ subsets: ["latin"], weight: ["500", "600", "700"], variable: "--font-display" });
+/*
+ * One typeface, matching the workspace. Both variables point at Inter so any
+ * rule still reading --font-display keeps working rather than falling back to
+ * a system font halfway down a page.
+ */
+const inter = Inter({ subsets: ["latin"], variable: "--font-sans" });
+const interDisplay = Inter({ subsets: ["latin"], variable: "--font-display" });
 
 export const metadata = {
   title: "BrokerStaffer — Onboarding",
@@ -14,7 +19,7 @@ export const metadata = {
 export default function RootLayout({ children }: { children: ReactNode }) {
   return (
     <html lang="en">
-      <body className={`${dmSans.variable} ${oswald.variable}`}>
+      <body className={`${inter.variable} ${interDisplay.variable}`}>
         <div className="topbar">
           <div className="topbar-inner">
             <a href="/" className="brand" aria-label="BrokerStaffer home">
